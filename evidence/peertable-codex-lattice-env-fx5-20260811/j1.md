# j1 証跡: Codex席と外部aiterm PTYの環境境界

実測日時: 2026-08-11T10:27:11Z
担当: asahi
Lattice CLI: 0.57.3
対象工程: `peertable-codex-lattice-env-fx5-20260811/j1`

## 実測

- Lattice 正本では j1 が `in-progress`（start sequence 1）。start の advisory は
  `conflicts_with_active=[]`。independence は `coverage=missing` のため、独立性を
  「検証済み」とは扱っていない。
- 着席中の Codex process（`.team/seats/asahi.json` の pid 81475）には、
  `PEERTABLE_MEMBER=asahi`、`PEERTABLE_PLAN=peertable-autonomy-runtime-20260811`、
  `LATTICE_CLI=/opt/homebrew/lib/node_modules/@quolu/lattice/bin/lattice.mjs`、
  `LATTICE_TODO_ACTOR_HOST=mac`、`LATTICE_TODO_ACTOR_SESSION=asahi`、
  `LATTICE_TODO_ACTOR_AGENT=asahi` が存在した。
- 同じrepoで新規に開いた外部aiterm PTY `asahi-j1-resume-20260811` では、上記6変数が
  すべて未設定だった。`"$LATTICE_CLI" todo status --json` は `zsh: permission denied`
  で rc=126 となった。

## 判定とhandoff

Peertableの着席プロセスへの環境配達は成立している。欠落は Codex が外部toolとして
新規作成するaiterm PTYへの環境継承で、Peertableの `launch-seat.sh` の修理対象ではない。
したがって本taskではPeertable本体を変更せず、aiterm/dotagentsのtool-environment工程へ
移管する。j1はこの外部修理と、修理後の実Codex席claim smokeが完了するまで未完了とする。

## akari引継ぎ後の実測

実測日時: 2026-08-12T00:28:16Z〜00:29:41Z
担当: akari

前節の「Peertable本体の修理対象ではない」というhandoff判定は、room [539] の所有裁定を受けた今回の実測で更新した。Peertable側の正規入口へ、外部aiterm MCPへ渡すsession環境を注入する。

- room [578] で、asahiの安全停止と j1 の `in-progress` / `start=1` を確認して引継ぎを宣言した。既存の start は二重に発行していない。
- `skill/scripts/launch-seat.sh` の Codex・Lattice modeに、`mcp_servers.aiterm.env_vars` として次の6名だけを渡す変更を加えた。`PEERTABLE_MEMBER`、`PEERTABLE_PLAN`、`LATTICE_CLI`、`LATTICE_TODO_ACTOR_HOST`、`LATTICE_TODO_ACTOR_SESSION`、`LATTICE_TODO_ACTOR_AGENT`。room MCPのclosed envとcredential値は拡張していない。旧setup-stateで `lattice_cli` が無い場合は `LATTICE_CLI` または `lattice` を使う。
- `bash -n skill/scripts/launch-seat.sh` は成功した。
- `node experiments/codex-lattice-env-repro.mjs` は欠陥版／修正版の比較、子processへの6値配達、room MCP境界、credential非露出、CLI fallbackを含め `6/6 green` だった。
- 既存回帰 `node experiments/launch-brief-repro.mjs` は `10/10 green` だった。
- 実 Codex CLIで `mcp_servers.aiterm.env_vars` を指定した `codex mcp get aiterm` は rc=0。6変数が masked env として表示された。
- 実 Codex一時席 `akari-j1-smoke` を launch-seatで起動し、room登録・seat-status-bridge起動まで成功した。Codex paneから実測した値は `PEERTABLE_MEMBER=akari-j1-smoke`、`PEERTABLE_PLAN=peertable-autonomy-runtime-20260811`、source-tree `LATTICE_CLI`、actor host=`mac`、session/agent=`akari-j1-smoke`。同じpaneで `${LATTICE_CLI:-lattice} todo status --json` の成功も確認した。
- smoke席は `leave-seat.sh` が `SEAT_LEAVE_OK` を返し、room member・aiterm session・seat identity・credentialの不在を読み返した。
- 既存 active taskを横取りしないため、実席での `todo start` は行っていない。readyも空であり、claim smokeは既存startを壊さない範囲の席環境・status実測として閉じた。

commit / push / Lattice done は room [629] の指示により、a3/k1のLattice closeとrepo HEAD固定後まで保留している。

## 再開後のfocused再確認

実測日時: 2026-08-12T02:35:45Z
担当: akari

- room [776][779] を受け、既存のj1 claim/startを継承した。現行HEADは `ecbb36a`、Latticeのj1は `in-progress`（既存start、done_atなし）で、対象WIPは `skill/scripts/launch-seat.sh`、`experiments/codex-lattice-env-repro.mjs`、本証跡の3ファイルだけだった。
- `bash -n skill/scripts/launch-seat.sh`、`node --check experiments/codex-lattice-env-repro.mjs`、tracked対象の `git diff --check`、fixtureの末尾空白検査が成功した。
- `node experiments/codex-lattice-env-repro.mjs` は `6/6 green`、既存回帰 `node experiments/launch-brief-repro.mjs` は `10/10 green` だった。
- g1所有の `skill/SKILL.md`、f2所有の `skill/scripts/ensure-bridge.sh`、その他のj1外ファイルは変更していない。次に対象3ファイルだけをcommitし、本人以外へpeer auditを依頼する。
