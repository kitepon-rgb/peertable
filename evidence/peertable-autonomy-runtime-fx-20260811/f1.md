# f1 pull run 前提の `.lattice/runs/` gitignore 整備

## 何を作ったか

`.gitignore` へ `.lattice/runs/` を1行追加した（commit `da11173`）。

## どう確認したか

1. **欠陥再現（修正前）**: `lattice run start --selection pull --id <一意suffix> --plan peertable-autonomy-runtime-20260811 --equipment detached-worktree` を実行すると、`.lattice/runs/` が git の追跡対象（gitignore 対象外）であることを理由に

   ```
   {"schema":"lattice.cli_error.v2","code":"RUN_STORE_NOT_IGNORED","message":".lattice/runs/ がgit ignore対象ではない","detail":{"guidance":".gitignoreへ `.lattice/runs/` を追加してから再実行する"}}
   ```

   で rc≠0 失敗した（2026-08-11T07:22Z、nagi セッション実測）。

2. **修正**: `.gitignore` へ `.lattice/runs/` を追加、`git add .gitignore && git commit`（da11173、pathspec明示・単独ファイル）。

3. **修正後の再実測**: 同じ `lattice run start` を新しい一意 id (`peertable-autonomy-runtime-20260811-nagi-1786432948`) で再実行すると `outcome: "started"` で成功。続けて `lattice run intake --run .lattice/runs/peertable-autonomy-runtime-20260811-nagi-1786432948 --task t2` も `intervention.state: "none"` で正常に worktree を取得できた（同セッション実測、t2 の作業ログに記録済み）。

## 受入条件

- pull run 生成（`lattice run start --selection pull ...`）が `.lattice/runs/` の gitignore 未設定を理由に失敗しないこと — 確認済み。
- `.gitignore` 以外のファイルを触らないこと — 確認済み（対象限定 commit da11173 は `.gitignore` 1 ファイルのみ）。
