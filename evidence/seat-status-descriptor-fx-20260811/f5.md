# f5 0.3.8 の publish（親: bell・2026-08-11）

**H 操作。オーナー承認済み（2026-08-11「publish まで込みで承認する」）。f2 の受入が全通過してから実行した。**

## 経過

1. `package.json` 0.3.7 → 0.3.8（commit c611313）
2. `verify-release-commit.mjs`: **一度落ちた**——push 前だったので `origin/main` の祖先でなかった。
   push してから rc=0（`release commit c611313f98f8 is landed on origin/main.`）
3. `npm publish`: **prepublishOnly が止めた**。`version_consistency: package.json=0.3.8 /
   client.mjs=0.3.7 で食い違っている`。`room/client.mjs:14` の `MCP_VERSION` を 0.3.8 へ揃え（commit 71609ec）、
   diagnostics が `ready` を返すことを確認してから再実行。**ゲートが仕事をしたので、版の食い違ったものは公開していない**
4. `npm publish` 成功: `+ peertable@0.3.8`（29 files / 103.0 kB / shasum e8979d62…）

## 公開後 smoke

- `npm view peertable version` → `0.3.8`
- registry から `npm pack peertable@0.3.8` して展開し、**新規3ファイルが公開物に入っていること**を確認:
  `skill/scripts/tmux-socket.mjs` / `skill/scripts/ensure-bridge.sh` / `skill/scripts/seat-usage.mjs`

## WSL handoff（この端末では実測できない残り）

ChromeBlocker 端末で次を実測してもらう:

1. **WSL 側**で `npm install -g peertable@0.3.8`。**Windows 側だけの更新では直らない**
   （bridge は WSL の tmux を読むので WSL 側の版が正）
2. **skill 実体（`~/.claude/skills/peertable` の symlink 先）も更新する**。npm global と skill が別経路なので、
   片方だけだと「更新したのに直らない」になる
3. 一時手当3点を**撤去**してから受入する: `peer-codex` の別名 session ／ `peertable-bridge` tmux 内の手動常駐 ／
   `PEERTABLE_TMUX_SOCKET` の手渡し
4. 受入: ①席を立てた直後に `GET /members` へ `status` が入る ②`busy`/`busy_since` → `idle` が遷移する
   ③**30 分以上経過後も `status_at` が更新され続けている**

**macOS では nohup 版も死なない**（実測）。nohup が落ちる側の対照実験は WSL でしか取れないので、
③ は WSL 側でこそ意味がある。
