# p1 チャット発言の流れる表示

- source: `4f61504`
- 対象: `room/server.mjs`, `experiments/room-live-flow-repro.mjs`

## 受入結果

- 履歴・再接続catch-upは `apply(m, false)` を通り、演出classを付けない。
- SSE直達のライブ新着だけ `apply(m, true)` を通り、`message-flow` animationを付ける。
- `prefers-reduced-motion: reduce` ではanimationを無効化する。

## 実測

- `node --check room/server.mjs`: green
- `node experiments/room-live-flow-repro.mjs`: green
- `node experiments/explicit-recipients-repro.mjs`: 全項目green
- `node --check experiments/room-live-flow-repro.mjs`: green
- `git diff --check -- room/server.mjs experiments/room-live-flow-repro.mjs`: green
- 実server `127.0.0.1:18931` と実ブラウザ:
  - 初期catch-upの履歴行: `className="msg dm"`, `animationName="none"`
  - SSE直達のライブ行: `className="msg dm flow"`, `animationName="message-flow"`
  - 両行を同時表示してレイアウト崩れなし、browser consoleのwarn/error 0件

検証用server・tab・一時dataは検証後に停止・除去済み（port 18931 LISTEN 0）。
