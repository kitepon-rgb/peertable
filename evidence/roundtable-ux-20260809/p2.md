# p2 メンバー情報へeffortを表示 — 既存実装・再受入証跡

## 結論

p2の要求は、先行task `refit-add-20260808/t14` で既に実装・独立監査・terminal受理されていた。
重複実装は行わず、要求と既存commitを突き合わせ、現行HEADでも契約が維持されることを再実測した。

## 既存実装との対応

- `80ec984`: `skill/scripts/launch-seat.sh`
  - 席のeffortを引数で受け、`PEERTABLE_EFFORT`として席envへ渡す。
  - 着席後のmember登録にも、指定時だけ`effort`を載せる。
- `eb15aaa`: `room/client.mjs`、`room/server.mjs`
  - clientが起動のたびにvendor/model/effortをmember登録metaへ載せる。
  - serverは「渡された欄だけ更新・未指定欄は保持・joined_atは初回値」のupsertで保存する。
  - UIはメンバーチップのhover titleとtap popoverへ`effort <値>`を表示する。
- `evidence/refit-add-20260808/t14.md`
  - effort付き登録、名前だけの再登録、statusだけの追記、旧形式memberの正負を実processで確認済み。
- `evidence/refit-add-20260808/terminal-audit.md`
  - 配信client JSの構文修復、upsert規則、probe互換、popover実ブラウザ確認を独立監査し、t14を受理済み。

## 現行HEADでの再実測

一時data dir・local server（`127.0.0.1:18794`）を使い、次を確認した。

- `{vendor:"claude", model:"sonnet", effort:"high"}`で新規登録後、`{status:"busy"}`だけを再POST。
- `GET /members`はvendor/model/effortを失わず、statusだけを追加した。
- systemの参加発言は初回の1件だけで、metadata更新では増えなかった。
- 配信HTMLから抽出したclient scriptを`node --check -`: green。
- `node --check room/client.mjs`: green。
- `bash -n skill/scripts/launch-seat.sh`: green。
- local serverを停止し、一時dir`/tmp/peertable-p2.YHMnwo`を削除した。

以上により、p2の3面（effortの起動時伝達、member metaの保持、hover/tap表示）は現行HEADで成立する。
このtask固有のsource変更は不要だった。
