# k1 DM配達保証

担当: kanade
対象plan: `peertable-dm-delivery-fx4e-20260811`
対象task: `k1`
実装commit: `4f13019b9aa101a63450fd59edb16d2e379e4667`

## 受入結果

1. **後から追加した席の動的解決**
   - 固定argsの席一覧・Codex限定・pending先消しを廃止し、roomのcurrent member nameから唯一のlive descriptorを解決する経路へ変更した。
   - `node experiments/seat-token-resolution-repro.mjs` — `19/19 green`。

2. **bridgeの復旧と競合**
   - descriptor消失、部分再起動、複数bridge競合、catch-upを別状態として扱い、失敗後にlastSeqを進めず、復旧後に同一DMを二重起床しない。
   - `node experiments/wakeup-bridge-descriptor-recovery-repro.mjs` — green。
   - `node experiments/wakeup-bridge-partial-restart-repro.mjs` — green。
   - `node experiments/wakeup-bridge-competition-repro.mjs` — green。
   - `node experiments/bridge-catchup-repro.mjs` — green。

3. **配達状態の分離**
   - 保存成功、wakeup成功、turn開始、read_unread取得を別状態で記録し、宛先不在・注入失敗・turn未開始・既読不明を成功へ丸めない。
   - `node experiments/dm-delivery-sequencing-repro.mjs` — green。
   - `node experiments/dm-delivery-cursor-race-repro.mjs` — green。

4. **実Codex席のsubmit**
   - 同一pane再試行時の半入力を `send-keys C-u` で消去し、本文のliteral送信とEnter submitを分離した。
   - `K1_REAL_VENDOR=codex K1_ACTION_LOOP=1 node experiments/dm-delivery-real-seats-repro.mjs` — `rc=0`。DM保存→一回wake→turn開始→read_unread→action room.postまで確認。
   - Claude席については、Mioによるread-only peer audit [604] が既存のreal Codex/Claude delivery green証跡を照合し、欠陥なしと判定した。手元での追加Claude再試行はlogin未認証で実行不能だったため、現時点の実行済み結果としては混同しない。

5. **外部adapterと配布面**
   - Bell external adapter、skill bundle必須capacity scripts、production preflightを確認した。
   - `node experiments/skill-bundle-capacity-repro.mjs` — green。
   - `node experiments/dm-delivery-production-preflight.mjs` — green。
   - `node --check room/client.mjs skill/scripts/wakeup-bridge.mjs skill/scripts/seat-usage.mjs experiments/seat-token-resolution-repro.mjs experiments/dm-delivery-real-seats-repro.mjs` — `rc=0`。
   - `bash experiments/bridge-supervisor-repro.sh` — `5/5 green`。

## 監査

Mioのread-only peer audit（room [604]）で、commit `4f13019`、元plan 7受入、commit内fixtureを実物照合した。判定: defect-free（PASS）。
seat-token `19/19`、DM保存→current descriptor解決→Codex一回wake→turn開始→read_unread、cursor race、SSE/heartbeat sequencing、descriptor復旧、partial restart、複数bridge競合、catch-up、member-turn、skill bundle、production preflight、実Codex/Claude DM deliveryを確認済み。途中失敗はpending/typed failureへ残り、重複wakeを生成しない契約も確認済み。

`4f13019` の `skill/scripts/launch-seat.sh` には並行c1の `PEERTABLE_MEMBER` 継承拒否ガードが同乗している。Mio [604] の分類どおり、これはk1機能欠陥ではなく、変更を戻さない。

## 受入の境界

本証跡は実装commitと監査PASS、focused fixture、実Codex席smokeの結果を束縛する。共有worktreeの未監査差分は含めず、MS-A2 deployと公開room smokeは、bell [614] 指示に従いclean一時worktreeで後続実施する。
