# h6 Claude→Codex vendor交代の原子性

## 対象

- plan: `peertable-tooling-friction-fx4c-20260811`
- task: `h6`
- 受入対象: Claude→Codexをfirst-classな席設定変更として扱い、live preflight、busy時の副作用ゼロ、target起動失敗時の旧vendor/model/effort rollback、成功時のmetadataとroom履歴を一つの操作で成立させる。
- 所有範囲: `skill/scripts/change-seat.sh` とそのfocused fixture `experiments/seat-change-repro.mjs`。
- h7、h8、k2、および他pathは対象外。

## 着手時の正本確認

- task detailを正本とし、Claude→Codexの明示的な`--vendor`・`--model`・`--effort`、target live catalog、busy拒否、target起動失敗時の旧設定rollback、metadata/historyを受入条件とした。
- `lattice todo independence` は `coverage=missing`、`active_task_ids=[h6]`、`conflicts_with_active=[]`、`guidance=independence_no_ready_frontier`。独立性を検査済みとは扱わず、会話調整として着手した。
- `lattice todo start` は sequence 5、`conflicts_with_active=[]`。h6 task noteは空で、plan最新note sequence 5はh7の訂正内容だったため、h6の根拠には使用していない。

## 実装

初回実装commitは`6297146`。次の2 pathだけを変更した。

- `skill/scripts/change-seat.sh`
  - target vendorを`claude`/`codex`として解析し、vendor交代時はmodelとeffortの明示指定を要求。
  - busy判定をtarget catalog検証より前に行い、busy席を停止しない。
  - ClaudeのeffortとCodexのmodel/effortを実CLIのlive catalogで検証。
  - `launch-seat.sh`のtarget起動失敗時、旧vendor/model/effortで明示rollback。
  - 成功後のmember metadataをvendor/model/effort全一致で読み返し、履歴を保存。
- `experiments/seat-change-repro.mjs`
  - Claude→Codex成功、busy、未知vendor、起動失敗rollback、live catalog、metadata/historyを再現するfixture。

独立監査で、成功後のmetadata readbackがmodel/effortだけでvendorを比較していない欠陥が見つかった。修正commitは`6e81533`で、Git上の直親は共有枝の`fbf7754`、`6297146`は祖先である。`6e81533`の変更pathは上記2 pathだけで、以後の対象2 path差分はない。

修正内容は次のとおり。

- member metadata readbackをvendor/model/effortの三項全一致へ変更。
- targetがCodexでもmetadataだけ旧Claudeを返す負例をfixtureへ追加し、履歴確定前のtyped failureを検証。

## 実装後のfocused test

```text
bash -n skill/scripts/change-seat.sh
node --check experiments/seat-change-repro.mjs
node experiments/seat-change-repro.mjs
seat-change repro: 27/27 green
git diff --check -- skill/scripts/change-seat.sh experiments/seat-change-repro.mjs
```

## 文脈近接の別席による実物監査

別席`h6audit`が変更・commit・evidence・Lattice書込を行わずに監査した。初回差戻しはroom [307]、修正後の最終所見はroom [313]で、結論は合格（defectなし）。

監査は`6e81533`の対象2 pathだけを実物照合し、h7/h8/k2と他pathを除外した。なお、監査中に確認された共有枝の未整理変更（`.lattice/todo/manifest.json`等）は対象外で、対象2 pathはcleanだった。

### 実測

- focused harness: `node experiments/seat-change-repro.mjs`、`27/27 green`。
- 独立一時roomの旧vendor metadata負例: target引数は`codex/gpt-5.6-luna/max`、member metadataは`claude/gpt-5.6-luna/max`。結果は`rc=1`、`SEAT_CHANGE_CHANGED_BUT_UNVERIFIED`（`vendor=codex`）、stdout空、`SEAT_CHANGE_OK`なし、room変更履歴0件。memberは旧Claudeのまま。
- 成功Claude→Codex: metadata vendor/model/effort全一致、room履歴1件。
- busy vendor交代: launch増加なし。
- target起動失敗: 旧vendor/model/effortへ一回rollback。
- live catalog: `claude --help`の`low/medium/high/xhigh/max`、`codex debug models`の`gpt-5.6-luna=max`。
- `bash -n`、`node --check`、commit diff-check、対象path clean、`6e81533`から対象pathの差分なし、`6297146`が`6e81533`の祖先であることを確認。

fixtureのroom履歴確認は実際の`/api/fixture/messages` HTTPレスポンスを読み、Node fetchの一時的な接続リセット時もcurlでHTTP 200と有効JSONを確認した。後続のroom履歴assertionはgreenだった。

この証跡は、実装→差戻し修正→別席の実物再監査完了後に作成した。次工程はこの証跡を束縛したLattice `done` である。
