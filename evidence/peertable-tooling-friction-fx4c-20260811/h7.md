# h7 Peertable席の委譲入口境界

## 実装

- 実装commit: `9c1a297aa16a78a53375976fd6ea290b32bea82f`
- commit対象: `skill/SKILL.md`、`skill/templates/charter.md`、`skill/templates/member.md`、`skill/templates/member-standalone.md`、`experiments/seat-delegation-policy-repro.mjs`
- 生成済み `.team/roles/member.md` にも同じ境界を反映した。`.team/` は `.git/info/exclude` 管理の生成物なので、commitには含めていない。
- 親が円卓メンバーを作る入口をPeertable正式手順 `launch-seat.sh` のAiterm長寿命席に限定した。
- 正式着席後のメンバーはnative sub-agent、Aiterm外部agent、相談agent、自己実装を自由に選べ、親は二次委譲の手段を禁止・指定しない。
- 委譲先の子は自動的な円卓メンバーではなく、工程所有・統合・room報告は着席メンバーが保持する。

## 正本と独立監査

- Lattice最新note sequence 4のオーナー裁定（親の席作成と着席後の二次委譲を分離）を正とし、sequence 5の実装報告と現物diffを突合した。
- native read-only refuterが同じnote、現物diff、parent 2ファイル、member 3ファイル、focused fixtureを独立監査し、`status=pass` と判定した。
- 前回差戻しの2点（負制御の偽green、連結文字列による個別source欠落の見逃し）は、fixtureで各sourceへmutationを先に適用し、各ファイルを個別assertする形へ修正した。
- 監査前後の対象ファイルに子の変更はなく、refuter reportの`changed_paths`は空だった。

## 実測

- `node experiments/seat-delegation-policy-repro.mjs`: green、rc=0
- `node experiments/seat-delegation-policy-repro.mjs --without-policy`: AssertionError、rc=1（契約欠落を検出）
- `node --check experiments/seat-delegation-policy-repro.mjs`: rc=0
- `node experiments/parent-role-repro.mjs`: green、rc=0（fixture endpointのbridge警告は出たが、全assertionはpass）
- `node experiments/member-autonomy-role-repro.mjs`: green、rc=0
- 対象5ファイルの`git diff --check`: rc=0
- commit実物の`git show`: 上記5ファイルのみ、84 insertions / 44 deletions

live seat起動・実agentによる二次委譲・origin同期は、今回のfocused監査の非対象として実施していない。
