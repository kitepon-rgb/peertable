# h8 Web member avatar色識別の安定化

## 対象

- plan: `peertable-tooling-friction-fx4c-20260811`
- task: `h8`
- 受入対象: Web UIで表示するmember avatarの色を、近接した色の衝突を避けつつroom内で安定させる。
- 所有範囲: `room/server.mjs` のWeb UI色割当周辺、およびその実測fixture `experiments/member-avatar-color-repro.mjs`。
- h7の委譲境界とk2の変更は対象外。

## 着手時の正本確認

- task detailは「表示memberに固定された高コントラストpaletteを使い、paletteを使い切った場合は知覚的に最も離れた色を再利用し、同一roomのrefresh/log再表示で安定させる」契約だった。
- `lattice todo independence --plan peertable-tooling-friction-fx4c-20260811 --json` は `coverage=missing`、`guidance=coordination_conversation`、`conflicts_with_active=[]`。
- `lattice todo start` は sequence 2、`conflicts_with_active=[]`。h7の対象pathsとは分離されていた。
- 最新訂正noteは sequence 3（digest `bd10c1fa…`）。sequence 1/2のh8への誤帰属を訂正し、h8は元の色識別契約だけを維持するとしている。このnoteを正として実装した。

## 実装

実装commitは `af89d84`（`h8: Web avatar色割当を安定化`）。`git show`で確認した変更対象は次の2ファイルだけだった。

- `room/server.mjs`
  - 8色の固定paletteと白文字を導入。
  - room内のactive member名をソートして割り当て、既存割当を保持。
  - palette枯渇時は既使用色のうち最小距離が最大となる候補を選ぶ。
  - messageのcatch-upより先にmember割当を同期し、chipと発言行で同じCSS変数を使う。
- `experiments/member-avatar-color-repro.mjs`
  - 旧hash直接hueで近接するmember pair、palette距離、refresh順序安定性、枯渇時の遠距離再利用、catch-up順序を再現・検証するfixture。

## 実装後のfocused test

以下はすべて成功した。

```text
node --check room/server.mjs
node --check experiments/member-avatar-color-repro.mjs
node experiments/member-avatar-color-repro.mjs
member avatar color repro: green
git diff --check -- room/server.mjs experiments/member-avatar-color-repro.mjs
```

## 文脈近接の別席による実物監査

別席 `h8audit` が変更・commit・evidence・Lattice書込を行わずに監査した。claimはroom [278]、最終所見はroom [283]で、結論はgreen（defectなし）。監査時点で対象pathsの作業差分はなかった。

### 静的・生成物・数値実測

- `git show af89d84`: 対象2ファイル、116 insertions/5 deletions。
- `room/server.mjs` とfixtureのsyntax check、fixture、対象限定 `git diff --check`: green。
- 旧実装で近接するpair: `seat-38,seat-39`、`old_hue_distance=1`。
- 新paletteのactive最小距離: `active_min_palette_distance=45`。
- refresh順序安定性: `refresh_order_stable=true`、`palette_count=8`。
- 枯渇時の最遠候補: `farthest_reuse_distance=90 best=90`。
- 最初の8席の重複なしと枯渇後再利用: `exhaustion_unique_first=8 reused_after_exhaustion=true`。
- catch-upの割当先行: `catch_up_refresh_before_message_apply=true`。
- 一時roomから生成したHTML: `generated_html_status=200 content_type=text/html avatar_palette_css=true refresh_before_apply=true bytes=23383`。

### 実ブラウザ確認

一時roomを実サーバーで起動し、Chromeでmember chipとcatch-up messageを確認した。

- chipは `alice=0/#a92b25`、`bob=180/#006b73`、`carol=90/#4d6b18` で重複なし。
- 発言行は `[4] alice → [5] bob` の順でchipと同じstyleだった。
- reload前後のstyle属性は完全一致（`refresh_stable=true`）。
- browser warning/errorは0件。
- 監査後に一時server・一時data・browser tabを片付け、対象paths cleanを再確認した。

## h8対象外の既存失敗

監査で次の既存fixtureも実行したが、h8差分外の既存期待により失敗した。

- `node experiments/member-activity-fx-repro.mjs`: rc=1、line 15のreduced-motion期待。
- `node experiments/room-live-flow-repro.mjs`: rc=1、line 10のlive-flow class期待。

監査席が `af89d84^` とdiffを静的比較し、両失敗箇所とh8のavatar CSS/palette/helper/render/refresh/catch-up hunkは非交差であることを確認した。したがって修正せず、h8の欠陥とは扱わない。

この証跡は、実装→別席の実物監査の完了後に作成した。次工程はこの証跡を束縛したLattice `done` である。
