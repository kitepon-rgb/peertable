# a3 本当の終了へ自動アナウンス

## 実装

- 対象は `skill/templates/done.sh` と `experiments/done-receipt-gate-repro.mjs` の2 pathだけ。
- `done.sh` は defect-free 監査所見、Lattice ToDoの done 状態、f6 の accepted receipt、task receipt の canonical landing、canonical upstream landing を確認した後だけ completed task event を送る。
- 同じ done の再試行は `done_at` 由来の同一 `transition_id` で冪等化し、reopen 後は新しい done transition として一度送る。失敗段階では completed event を生成しない。
- k1 の `room/client.mjs` と `skill/scripts/wakeup-bridge.mjs` は変更していない。

## 文脈近接の別席監査

Kanade が実装者と別席で commit `1e0b503` の現物差分、元planの完了条件、focused harness を read-only peer audit し、room [590] に「現時点でdefectなし」と記録した。監査では未accept、監査/evidence不足、f6 task receipt未着地、canonical landing前、retryの同一transition冪等、reopen後の別transition、非実行層・未runの素通し、run状態読取失敗時の拒否を確認している。

## 実測

- 実装commit: `1e0b503`（対象2 path限定）。
- focused harness: `node experiments/done-receipt-gate-repro.mjs` は `29/29 green`。
- `structure realize --planned --commit HEAD` は成功し、realization ref は `.lattice/todo/plans/peertable-task-announcements-20260811/v1/structure/realizations/a3.jsonl`。
- room報告・peer audit依頼は [583]、Kanadeの監査所見は [590]。

publish・本番deployはこの証跡作成時点では実施していない。canonical landing と `todo done` の後段で実施する。
