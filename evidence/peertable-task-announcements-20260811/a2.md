# a2 正規着手入口と started 通知

## 実装

- 実装commit: `9ba12cedcb087d9e2f6cdfbeb51e519d2e6eb40f`
- 対象は `skill/scripts/setup.sh`、`skill/templates/start.sh`、`skill/templates/start-event.mjs`、`experiments/task-start-repro.mjs` の4ファイルだけ。
- setupがLattice／standaloneの両モードへ正規 `start.sh` と event helper を配布する。
- Latticeは `todo start` の引数を保持し、成功後だけ `started` typed task eventを送る。失敗時は非0で終了し、通知しない。
- standaloneは `.team/tasks.md` からtitleを解決し、roomの最新 `[claim]` をtask token完全一致で照合する。同じclaimの再試行は `room・actor・task・claim_seq` から同じtransitionになり、新しいclaimだけ新transitionになる。
- credential tokenはcredential fileからhelper内だけで読み、argv・環境変数・標準出力／標準エラーへ載せない。

## 独立監査

- Lattice note sequence 3〜8を正本として、実装diff・fixture・関連setup fixtureをnative read-only refuterが再監査した。
- r4監査はa2所有範囲を `status=pass` と判定した。claim完全一致、同一claim冪等、新claim遷移、全start経路のcredential非漏洩、setup配布、引数保持、成功後通知、失敗時無通知、standalone option拒否を現物で確認した。
- 監査前後の対象7ファイルに子の変更はなく、refuter reportの `changed_paths` は空だった。

## 実測

- `node experiments/task-start-repro.mjs`: green、rc=0。Lattice成功、`--parallel-frontier` 保持、失敗rc=7・無通知、standalone成功、同一claim再試行、新claim、`solo-a`／`solo-aa` 部分一致拒否、option拒否、全start stdout/stderrおよび全mode子process argv/envのcredential非漏洩を確認。
- `node experiments/parent-role-repro.mjs`: green、rc=0。fixture endpoint由来のbridge警告は出たが、全assertionはpass。
- `bash -n skill/scripts/setup.sh && bash -n skill/templates/start.sh`: rc=0
- `node --check` を `skill/templates/start-event.mjs`、`experiments/task-start-repro.mjs`、`experiments/parent-role-repro.mjs`、`room/client.mjs`へ個別実行: すべてrc=0
- 対象4ファイルの `git diff --check`: rc=0
- `git show` で実装commitが上記4ファイルだけであることを確認した。

## 切り分け

`room/client.mjs` の `skill_bundle` required listに `templates/start.sh` と `templates/start-event.mjs` がないため、diagnosticsが配布漏れを検出しない問題はr4でも確認された。ただし当該pathはk2所有中の競合pathであり、a2の所有・変更・受入範囲から分離してroom [312]で統合要求を報告した。k2修正後のdiagnostics負制御、live seat、実Lattice統合、origin同期、npm pack／publishは今回の非対象である。
