# a4 受信後に返信せず自律ループへ戻る role

## 実装

- 対象は `skill/templates/member.md`、`skill/templates/member-standalone.md`、`skill/templates/parent.md`、`experiments/task-event-role-repro.mjs` の4 pathだけ。
- Lattice `started` / `completed` task eventを会話の返信要求や配車と区別し、member roleは状態を読み直して active → ready → 文脈近接 peer audit → 待機へ戻る規律を明記した。
- `了解`・受取・追認などのack投稿、イベントtaskの横取り・再claimを禁止した。`completed`後は新しく開いたreadyと監査候補を探す。
- parent roleは `started` では観測だけに留まり、通常のLattice工程管理・配車・peer audit不足補充を行わず、`completed` 後だけ黙って工程完了後照合へ入ることを明記した。
- setupを使うfixtureで、Lattice modeのmember、standalone modeのmember、parentの三生成roleをtemplateと突合した。transportと既存scriptは変更していない。

## 文脈近接の別席監査

h6auditが実装者と別席で対象4 pathの現物diff、受入条件、fixtureを監査し、room [485] にdefect-free所見を記録した。監査では次を確認している。

- `node experiments/task-event-role-repro.mjs` は rc=0。3生成roleのtemplate一致、started/completed後の状態再読、ack/追認禁止、イベントtask横取り禁止、active → ready → 文脈近接 peer audit → 待機、parentのstarted観測限定・completed後のみ黙った照合が全てgreen。
- `node experiments/member-autonomy-role-repro.mjs` は rc=0でgreen。
- 対象4 path限定のunstaged/staged `git diff --check` はともにrc=0、fixture末尾空白なし。変更・commit・Lattice書込は監査席では行っていない。

## 実測

- `node --check experiments/task-event-role-repro.mjs`: green
- `node experiments/task-event-role-repro.mjs`: rc=0、全判定green
- `node experiments/member-autonomy-role-repro.mjs`: rc=0、全判定green
- `git diff --check`（対象pathのunstaged/staged）: green
- Lattice着手: sequence 9、event digest `e47f101ce5ab03144e8c42a5fe5d47a1af0bc5627f0b6de273773f9858694e07`

publish・本番deployは実施していない。共有worktreeには他席の未受入WIPとremote divergenceがあるため、a4対象外の統合・pushは行わない。
