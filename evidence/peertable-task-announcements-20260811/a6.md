# a6 完了証跡 — 通知機構を通常postの4規則へ縮小

## 結論

旧a6が予定したtyped task eventの統合は行わない。工程通知は既存の通常room発言だけで実現し、
専用event、二重の着手通知、自動completion、遷移ID、送信者除外、member turn completedを撤去した。

## 現行仕様

1. Claim: `post(to: "all")`
2. 工程完了: `post(to: "all")`
3. 発言ターン終了: 次の行動を自分宛DM
4. 個人への用事: その人宛DM。宛先不明の相談: `post(to: "all")`

## 成果

- `5524b84` — room通知をtyped eventから通常postへ戻した。
- `483b205` — 規則を上記4つへ限定し、親DM三分類、複数宛先禁止、旧自動通知の残骸を撤去した。
- `55cd846` — MS-A2のproduction imageを確定した。
- 配備image: `peertable-room:20260812-5524b84`

## 実測

- LAN / 公開URLともHTTP 200。
- room seq `1037`: bellから`all`への通常発言を全席のwakeup配送が受領。
- room seq `1038`: nagiからnagi自身へのDMをnagiのwakeup配送が受領。
- `room-routing-repro`、`team-generated-assets-upgrade-repro`、`codex-parent-delivery-repro`、
  `done-receipt-gate-repro`、`node --check` がgreen。

## 旧工程の扱い

a1〜a5とu1は、typed通知と自動入口を試作・配備した履歴としてdoneのまま残す。現行仕様は本証跡の
4規則で置換されており、旧機構をa6で完成させる作業は行わない。

## 完了判断

a6は、不要機構を増設する統合工程ではなく、最小仕様への縮小、旧機構の撤去、配備、room/wakeup実測を
受理する工程として完了する。
