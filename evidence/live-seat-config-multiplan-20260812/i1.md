# i1 — 旧 t4 を保持した同一円卓での統合実測

実施日: 2026-08-12

## 同一 room・複数 PLAN

- 現在の room `peertable-autonomy-runtime-20260811` で、Lattice store に対し
  `lattice todo show --plan live-seat-config-multiplan-20260812 --task i1 --json` と
  `lattice todo show --plan peertable-autonomy-runtime-20260811 --task t4 --json` を実行した。
- 本 PLAN の `i1` は `in-progress`、旧 PLAN の `t4` も `in-progress` のまま、
  `t4` の evidence は `null` だった。完全修飾した別 PLAN の read-only 操作だけで、
  同じ store・同じ room から本 PLAN に到達できた。
- `lattice run list --json` は旧 PLAN の pull run
  `peertable-autonomy-runtime-20260811-nagi-1786432948` が継続中であることを返した。
  i1 は旧 PLAN の task、run、evidence を更新するコマンドを実行していない。

## Aiterm の live configure

- 破棄可能な読み取り専用 Codex session
  `i1-configure-fixture-c-20260812` を `gpt-5.6-terra` / `low` で起動した。
  launch receipt は `write_scope_enforcement: enforced_read_only` を返した。
- 同じ session に対して
  `node skill/scripts/aiterm-configure.mjs i1-configure-fixture-c-20260812 --model gpt-5.6-luna --effort medium`
  を実行し、`aiterm.agent-configure-result.v1` receipt
  （provider=`codex`、model=`gpt-5.6-luna`、reasoning_effort=`medium`）を得た。
- session を再起動せず `pty_send` し、同一 session の transcript から
  `READY I1-CONTEXT-7391` を回収した。最後に `pty_close` は `outcome: closed` を返した。
  c2 の実席証跡にある configure 前後の文脈継続実測と合わせ、同じ Aiterm 公開経路を
  本 PLAN と旧 t4 が共存する現在の room でも利用できることを確認した。

## 最終自己監査

- i1 の所有変更は本 evidence だけである。旧 t4 所有の
  `experiments/autonomy-lifecycle-real-repro.mjs`、旧 PLAN の evidence、既存 run には書込みをしていない。
- 旧 t4 の Lattice readback は開始時・終了時とも `in-progress`、evidence `null` であり、
  active run も同じ run id のまま確認できた。
