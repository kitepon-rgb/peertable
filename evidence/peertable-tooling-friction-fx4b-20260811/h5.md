# h5 archive-room-log と teardown の Unicode room path

## 実装

- 実装 commit: `ef0ecc8`
- 修理 commit: `0591584`
- 対象: `skill/scripts/archive-room-log.py`、`skill/scripts/teardown.sh`、`experiments/teardown-room-url-repro.mjs`
- `archive-room-log.py` の取得 path と `teardown.sh` の解散 POST path を API path として percent-encode した。
- ASCII room の挙動を維持し、日本語 room `卓-あ` でもログ控えの取得、member 解除、archive 保持を通す。
- 変異実行時に `process.exit()` で `finally` の一時 dir cleanup を飛ばさないよう、`process.exitCode` に修理した。

## 文脈近接の別席監査

担当外の別席 `h5audit2` が実物監査した。room log の [242] に、修理後の正系・2変異・temp cleanup を実測し、コード変更・工程 claim なしと記録されている。

初回の別席監査 [235] は、変異時の一時 dir 残存を検出して保留した。指摘を [237] で共有し、`0591584` へ修理後、再監査で解消を確認した。

## 実測

- `bash -n skill/scripts/teardown.sh`: rc 0
- `archive-room-log.py` の Python syntax check: rc 0
- `node --check experiments/teardown-room-url-repro.mjs`: rc 0
- 正系 fixture: ASCII `ascii-room` と日本語 `卓-あ` の URL 表示・member 解除・archive 保持が green
- `--without-unicode-path`: rc 1、Unicode path 変異を検出
- `--without-boundary`: rc 1、byte 完全な room URL 変異を検出
- 2変異の実行後、対象の `peertable-teardown-room-url-mutant-*` / `peertable-teardown-room-url-*` 一時 dir 残存なし
- 対象 diff の `git diff --check`: green

publish・本番 deploy は実施していない。
