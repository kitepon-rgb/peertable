# t3 証跡 — consumer contract 4面の明文化（決定46）

担当: akari / plan: roundtable-improvement / 2026-08-08

## 何を作ったか

決定46で「未実施」として残っていた consumer contract を、両側の文書へ明文化した。

**peertable `docs/plan.md`（+52行・追記のみ）**

- 新設 §12「Lattice consumer contract — Peertable が消費する4面」
  - 12.1 依存付き ready 一覧 / 12.2 順序付き start/done 記録 / 12.3 証跡束縛 / 12.4 監査状態
  - 各面につき「入口（実コマンドと schema）」「消費する欄」「Peertable 側の用途」「契約として Lattice に要求するもの」の4点で書いた
  - 12.5 に単独円卓モードの位置づけと面ごとの代替表（決定47・50 と接続）
- §4.3（計画: Lattice）の末尾へ §12 への参照1行

**Lattice `docs/00_product-contract.md`（+28行・追記のみ・1 commit）**

- 末尾へ「消費者としてのPeertable（外部consumer contract）」節。Lattice 側の文体（である調・schema/CLI 名）に合わせ、
  **新しい保証・非目標・面を増やさない記録である**と冒頭で明示した（消費者のために製品契約が膨らむのを防ぐため）

## どう確認したか

記述の各主張を実物で照合した。モデルの記憶や既存文書の孫引きは根拠にしていない。

| 主張 | 照合方法 | 結果 |
|------|----------|------|
| 面1 の欄名（`next_ready`/`blocked`/`active_set`/`dispatch_frontier`・`policy: all_ready_parallel_by_default`・`recommended_parallelism`・`frontier_digest`） | `lattice todo status --json` の実出力 | 全て実在。schema は `lattice.todo_status_result.v6` |
| 面2 の journal 構造（`sequence`・`actor{agent,host,session}`・`previous_digest` 連鎖） | `.lattice/todo/plans/roundtable-improvement/v1/journal/active.jsonl` を直読み | 実在。genesis(seq 0, actor bell) → t3 start(seq 1, actor akari) → t1 start(seq 2, actor tsumugi) が digest 連鎖で並ぶ |
| 面2 の「task に assignee を持たない」 | 同 journal と `lattice todo show --plan … --task … --json` の `task`/`state` を確認 | assignee 相当の field は無い。着手者は event の actor としてだけ残る |
| 面2 の actor env（`LATTICE_TODO_ACTOR_HOST`/`_SESSION`/`_AGENT`） | メンバーセッションの `env` 実測 | 3つとも設定済み（session=akari, host=mac, agent=akari） |
| 面3 の descriptor 構成（`evidence_id`/`repo_id`/`path`/`git_blob_oid`/`content_digest`/`media_type`/`anchor_digest`）と生成方法 | `.team/scripts/done.sh` の実物 | `git hash-object -w` と `shasum -a 256` から生成している。repo 外絶対 path が `INVALID_ARGUMENTS` で弾かれる旨もコメントに実記録あり |
| 面4 の「全 task done は `gate_ready` であって完走ではない」「監査待ちが在る限り残作業なしと答えない」 | Lattice `docs/00_product-contract.md` の当該節（ADR 0147・0159） | 記載どおり |
| 面4 の `lattice status --json` の `next_action.reason: audit_pending` | **実観測はしていない**。現在は ready/active があるため `active_run_present` が返る（実出力で確認） | 文書上の契約として引用。監査待ち状態の実観測は未了 |

- 追記のみであることの確認: `git diff --stat` が peertable `docs/plan.md` 52 insertions / 0 deletions、Lattice `docs/00_product-contract.md` 28 insertions / 0 deletions
- 境界の遵守: §8 決定履歴・§11・§9.7 には触れていない（t4 の面）。`skill/` 配下にも触れていない（t1 の面）。room [10][13] で申し合わせ済み

## 判断と申し送り

- **§12 の置き場を末尾の新節にした理由**: 内容的には §4.3（計画: Lattice）の直下が自然だが、そこへ節を挿すと以降の番号が全てずれ、
  同時進行の t4（正典整理）と衝突する。番号を動かさずに済む末尾追記とし、発見性は §4.3 からの参照1行で担保した
- **単独モードの記述は t1 の実装が正本**: 12.5 の表は campaign-plan と つむぎの [12] 宣言に整合させたが、実装で意味が変わったら文書側を私が直す
- **Lattice 側で気づいた既存の drift（今回は直していない）**: `docs/00_product-contract.md` は mutation 結果を
  `lattice.todo_mutation_result.v2` と書いているが、実CLI（v0.48.0）が返すのは v4、文書の題も「製品契約（0.37.0）」のまま。
  Lattice 製品側の課題であり、本 task の「Peertable 節を1 commit 追記」の範囲外なので手を触れていない。§12 では版数の断定を避け、
  観測した欄名だけで書いた
