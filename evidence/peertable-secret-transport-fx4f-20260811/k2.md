# k2 書込トークンを画面・argv・transcriptへ出さず席へ渡す

## 実装

- 実装commit: `ae42721ad2a85bd1488165af12aa496e442b9ad8`
- 通常退席cleanup追補commit: `74abedd`
- 独立監査差戻し修正commit: `7859fa394d9886640f957c09478b2a7a63b5b4b1`
- 独立再監査差戻し修正commit: `fbf7754ed8e4dd7cea6691aea35d058e114f8ed7`
- socket unlink負例の差戻し修正commit: `b9c797758f4eb6bca74c36f237615556e64f8579`
- `seat-credential.mjs` が `~/.config/peertable.env` を唯一の値解決面として読み、席・roomごとに分離したruntime credential fileを原子的に作る。directoryは`0700`、fileは`0600`。
- launcher・seat-change・Codex/Claude clientへ渡すのは`PEERTABLE_CREDENTIAL_FILE`のpathだけで、token値をshell入力、起動argv、子process環境へ載せない。
- clientはcredential fileを必須として直接読み、path欠落・読取不能・空fileをtyped errorとして拒否する。`PEERTABLE_POST_TOKEN`へのfallbackは行わない。
- CodexのMCP起動とClaude用templateは、既存global installでなく同じPeertable treeの`room/client.mjs`を使う。Peertable管理下の既存`.mcp.json`もlaunch時に原子的に同期し、ユーザー所有fileは無断変更せずtypedに止める。
- tmux serverのglobal envに旧tokenが残っていても、新sessionは`PEERTABLE_POST_TOKEN`を空で上書きして子shellへ再注入しない。launcher自身も正規手順を`env -u PEERTABLE_POST_TOKEN`に固定した。
- 起動rollback、room設定変更、通常退席、teardownでruntime credentialを撤去する。通常退席は`leave-seat.sh`がsession、room member、seat identity、credentialを同じ境界で片付ける。

## 実席smoke

実Codex席`k2-codex-smoke`と実Claude席`k2-claude-smoke`を起動し、次を実測した。

- credential directory `0700`、両席のcredential file `0600`
- 両席のmain processと子孫processのargv／`ps eww`、tmux全scrollback、seat identity、通常`.team` log、room logにsentinel値なし
- Codexの投稿成功: room [273]、inbound DMの`read_unread`と返信成功: room [279]
- Claudeの投稿成功: room [274]、inbound DMの`read_unread`と返信成功: room [277]
- smoke後は両tmux session、room member、seat identity、対象credentialを撤去し、runtime `.mcp.json` を変更前の内容へ復元

## focused実測

- `node experiments/seat-secret-transport-repro.mjs`: green
  - 同room別席・同席別roomのpath分離、`0700`／`0600`
  - 管理下`.mcp.json`のcurrent-tree exact同期、余分なtoken env除去と他server保持、preexisting fileのtyped拒否
  - client argv／process envのsentinel陰性
  - DM送信と未読取得
  - path欠落／読取不能fileから平文envへfallbackしないtyped error
  - room設定変更、helper remove、通常退席後のsession／member／identity／credential残存ゼロ
  - session観測不能時は後段を消さずtyped failure
- `node experiments/launch-brief-repro.mjs`: 10/10 green
  - pane、session env、launcher出力、起動argv相当、seat identityのsentinel陰性
  - 起動rollback後のcredential残存ゼロ
  - 実tmux socketをunlinkしてserver生存を観測し、session観測不能時はmember DELETE／identity／credential削除を呼ばずtyped failure
- `node experiments/seat-model-liveness-repro.mjs`: 4/4 green
- `node experiments/seat-change-repro.mjs`: 22/22 green
- `node experiments/effort-change-repro.mjs`: 5/5 green
- `node experiments/task-event-transport-repro.mjs`: 19/19 green
- 独立tmux socketでglobal envにsynthetic sentinelを残してから新sessionを作り、`show-environment`が`PEERTABLE_POST_TOKEN=`、sentinel非継承: green
- `node experiments/seat-delegation-policy-repro.mjs`: green
- `node experiments/member-autonomy-role-repro.mjs`: green
- `node room/client.mjs diagnostics --json`: `overall=ready`、`skill_bundle=pass`
- `node --check room/client.mjs skill/scripts/seat-credential.mjs`: green
- `bash -n skill/scripts/launch-seat.sh skill/scripts/change-seat.sh skill/scripts/leave-seat.sh`: green
- `git diff --check`: green

## 独立監査

read-only監査席が全k2 commitとfocused harnessを再確認し、対象範囲をdefect-freeと判定した。

- `seat-secret-transport-repro`: green
- `launch-brief-repro`: 10/10 green
- 独立black-boxで実tmux sessionのsocketをunlink後に`leave-seat.sh`を実行し、非0終了、`SEAT_LEAVE_SESSION_UNREADABLE`、credential／identity保持、tmux server生存を確認
- launch／leaveともsocket path消失を成功扱いせず、member DELETE／identity／credential削除前に停止
- harness・black-box終了後の`peertable-launch-brief-*`／`peertable-k2-*`関連process残留ゼロ
- 既確認済みの権限、path分離、平文env fallback禁止、MCP exact同期、rollback、通常退席、diagnosticsに差分退行なし

`experiments/setup-runtime-exclude-repro.mjs`は、現行`setup.sh`がwork-order adapter登録を廃止済みなのに旧`registration.json`を期待する既存不整合でfailした。k2のcredential/template経路には到達せず、room [280]で別件として分離報告した。

publish・deployは実施していない。
