# t4: 手順書と計画書を実装の現況へ同期する

## 何を作ったか（`skill/SKILL.md` と `docs/plan.md` 単独所有・コードは触らない）

- (a) `skill/SKILL.md` の effort 必須化への同期。着手時点で聞き取り(1.)・メンバー起動(5.)は既に t2 の
  実装(`launch-seat.sh` の必須引数化)へ追従済みだった(過去の同期漏れではなかった)。今回追記したのは:
  - 6.5節「席の稼働状態ブリッジ」の説明へ4値目 `blocked`（判定順 busy→blocked→idle）を追加
  - 7.「親の着卓」へ、親の `effort` は任意のまま据え置く理由(親は自分の effort を機械的に知る経路が無い)を1行追加
- (b) `docs/plan.md` §15「円卓UX campaign」の実装現況同期。§15冒頭が「起票のみ」のまま放置され、実際には
  `roundtable-ux-20260809` plan で p2/p3/p5/p6/p7 が accept まで進んでいた(＝本 campaign `roundtable-ux2-20260810`
  の発端)。p1〜p7 それぞれへ、根拠の file:line を自分で開いて確認してから「現況」節を追記した:
  - p1: 発言全体の flow は既存着地(`room/server.mjs:255-257`)、t1 が追加したバブル内ブロック逐次出現
    (`:259-261`)を追記
  - p2: 登録経路(`client.mjs:124-129`)は既存着地、t1 の1行畳み込み表示(`:389-390`)と、t2 が塞いだ
    「Claude席のeffort欠落」(launch-seat.shの必須引数化)を記録
  - p3: `change-effort.sh` 着地済みと明記
  - p4: **据え置きのまま**と明記（token消費目安の表示は着地しているが、コストの金額換算は未実装）
  - p5: `seq()` によるログ番号表示の着地(`:365-366,377`)を記録
  - p6: 既存着地①②③に加え、本campaignで塞いだ2点(稼働表示の最大38秒遅れ→SSE即時押し込み、
    承認待ちのidle誤表示→blocked追加)を file:line 付きで記録
  - p7: bell宛DM番犬による着地(2026-08-09受入実測済み)を記録
- (c) `docs/plan.md` §11 backlog の同期: 「`busy`/`idle`/`dead` の3値では固着した席を表現できない」項目を
  今回消化したので消化済みへ移動。「心拍10秒・減衰30秒を対で刻む案」は今回も採らなかった旨と、その理由
  (心拍側を刻まず画面側=SSE押し込みで遅れを解いたため)を追記。
  （**この(c)は kotone との並行編集の結果が working tree に残っていたものを引き継いだ**。下記「経緯」参照）

## 経緯（claim衝突と解消・room記録どおり）

t4 は tsumugi([18] 13:33:04.321Z)と kotone([19] 13:33:07.071Z)がほぼ同時に room claim し、
`lattice todo start` も両者とも成立していた（tsumugi: sequence 6・kotone: sequence 7——**Lattice側の
二重start自体は監査事実として別途 room へ報告する**）。同じ working tree の `docs/plan.md`／`skill/SKILL.md`
を双方が並行して Edit していたため、tsumugi の Edit 実行時に「ファイルが外部で変更されている」警告が出て
発覚。room([20]-[23])で協議し、①room claim の先着(tsumugiが3秒早い)を機械の事実として採用 ②kotone の
未commit編集(SKILL.md 4箇所・plan.md §11/§15冒頭・p1)は内容が正しく本task の目的と合致していたため
**そのまま引き継いで**tsumugiがp2〜p7の残りとcommit/pushまで完了させる、で合意した。kotoneはcommitして
いない。

## どう確認したか

- `git diff docs/plan.md skill/SKILL.md` を通読し、file:line引用の根拠を`grep -n`で1件ずつ自分で開いて
  裏取りした（`room/server.mjs:255-257,259-261,389-390,400-401,365-366,377,243-244,440,443,82-88,173-174,246,249`、
  `room/client.mjs:124-129`、`skill/scripts/change-effort.sh`存在、`skill/SKILL.md`152行のbell宛DM番犬記述）
- `docs/plan_roundtable-ux2-20260810.md`「背景」節への相互参照が実在することを確認
- p4を「据え置き」と明記し、黙って消していないことを確認（実装しなかったものは実装しなかったと書く）
- コードは変更していないので `node -c` 等の対象なし。markdown の構文チェックツールは無いため、diffの通読で確認

## 見てほしい点

- kotoneとの並行編集内容(§11・§15冒頭・SKILL.md 4箇所・p1)をそのまま自分の成果として引き継いだ判断が
  妥当か（内容自体はkotoneも同じ設計メモを参照していたため実質重複だが、由来の透明性のため経緯節に明記した）
- Lattice の t4 二重start(sequence 6/7 双方成立)は本taskの受入条件外の別欠陥として、監査依頼と別にbellへ
  報告する予定
