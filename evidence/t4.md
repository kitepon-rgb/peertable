# t4 証跡 — 正典整理（決定50/51・§11 消化・§9.7・README）

担当: akari（§9.7・§11・README 2件・統合確認）／ tsumugi が [join] で §8 決定履歴の追記
plan: roundtable-improvement / 2026-08-08

分担と順序は room [25][27][29][31] で合意。**同一ファイル（docs/plan.md）を触るため直列化した**——
つむぎが §8 を先に書いて commit（`ac334fe`）、その後で私が §9.7・§11 に入った。
理由: `git commit -- docs/plan.md` は working tree のファイル全体を拾うので、pathspec ではセクション単位の
境界を守れない（つむぎの [27] の指摘。私の [25] は誤りだったので撤回した）。

## 何を作ったか

**tsumugi / `docs/plan.md` §8（commit `ac334fe`・+10行・削除0）**

- 決定50「単独円卓モードの形」——分岐だけ入れてトラッカーを作らない理由（代替物を実装した時点で
  「Lattice 非依存」ではなく「劣化 Lattice の内蔵」になり決定46 と矛盾する）、モード選択と `setup.sh` の受け口、
  議題表が状態を持たないこと、役割文書の差し替えとファイル名固定、憲章の抽象化、完走判定は親
- 決定51「席の縮退と再着任の手順化」——縮退5手順と順序の根拠2つ、再着任はメンバー自身の責務、親は齟齬だけ見る

**akari / `docs/plan.md` §9.7・§11（本 commit・+8 −10行）**

- §9.7 へ「次回実運用での観測」2件を新設（再着任・席の縮退）。**実地検証が不可能な理由も書いた**——
  context の要約は意図的に起こせず、縮退は frontier が細る形の campaign でしか観測できない
- §11 の「未検証のリスク」2件を §9.7 へ移動（§11 からは削除）
- §11 の「手順書へ反映すべき」2件と「約束済み・未実施」のうち実施済み2件を消化。跡を辿れるように
  **消化先を1ブロックにまとめて残した**（どの課題がどの決定・どの正典へ行ったか）。§11 に残るのは
  観察中3件（決定48 の最小主義どおり条文化しない）と Web UI 着せ替え1件だけ

**akari / `README.md`・`README.ja.md`（commit `ebc65c8`・+33 −6行）**

- 「二つのモード（Lattice 併用 / 単独）」の節を両言語へ。§12.5 と同じ4行（仕事の取り出し口・claim と完了・
  完了の束縛・完走の判定）で切り、単独で失うのは task 間スケジューリングの機械保証だけだと明記
- 三層表の「計画」行へ *任意* の但し書き、skill 説明へ単独時は `.team/tasks.md` を生成する旨
- 決定数 41 → **51**（決定50・51 の追加に合わせた。番号が 50/51 で合っていることは つむぎの [32] で確認）

## どう確認したか

消化したと書いた項目が、本当に反映先に在るかを機械照合した（「消化済み」と書いて実体が無いのが
この種の整理の典型的な失敗なので）。

| 消化した課題 | 反映先 | 照合 |
|---|---|---|
| 席数の既定＝`max_frontier_width` | `skill/SKILL.md` 聞き取り | `grep -c` → 1 |
| 席の残骸予防 | `skill/SKILL.md` setup 手順5 | `grep -c pty_list` → 1 |
| 席の縮退 | `skill/SKILL.md` 親 notes ＋ 決定51 | `grep -c 縮退` → 1 / 決定51 在り |
| compaction 再着任 | `skill/SKILL.md` 親 notes ＋ `templates/member.md` 両版 ＋ 決定51 | `grep -c 再着任` → 1（+ t2 で照合済み） |
| note/room の使い分け | `skill/templates/member.md` | `grep -c 二重化` → 1 |
| consumer contract 4面 | `docs/plan.md` §12 ＋ Lattice `docs/00_product-contract.md` | t3 で照合済み |
| 単独円卓モード | 決定50 ＋ `skill/` 実装 | `grep -c '^50\.\|^51\.'` → 2 |

- §9.7 の観測項目: `grep -c 次回実運用での観測` → 1
- §11 に残った箇条書き: **4件**（観察中3 ＋ Web UI 1）。移動・消化した6件が消えていること
- `git diff --stat docs/plan.md` → 8 insertions / 10 deletions（§8 の追記は別 commit なので含まれない）
- つむぎの §8 が私の §12 と食い違っていないことを実物で確認（決定50 は「4面に対応する」と参照しているだけで
  内容を再定義していない）

## 触っていないもの

- `docs/plan.md` §8（つむぎの面）／§12・§4.3 の参照行（私の t3 成果・今回は変更不要）
- `skill/` 配下（t1・t2 で確定済み）
- `package.json` の version（t5 の面）
