# t4 完了証跡 — 実円卓ライフサイクル統合実測

## 対象と成果

- plan: `peertable-autonomy-runtime-20260811`
- task: `t4`
- claim: room #1101
- 実測ハーネス commit: `99f847a` (`experiments/autonomy-lifecycle-real-repro.mjs`)
- r1 修理: `526ccc3`、r1 証跡: `3267142`
- 実測 room: `t4-live-9512`
- 実測時刻: `2026-08-12T08:04:54Z`〜`2026-08-12T08:09:19Z`

使い捨て room / project、実 Codex 席 `t4-live-worker`（`gpt-5.6-sol / high`）、実
`launch-seat.sh`、`wakeup-bridge.mjs`、`change-seat.sh`、`leave-seat.sh` を一連で使用した。
`change-seat.sh` は `526ccc3` の履歴POST応答seq束縛・GET読返しを含む現行版で実行した。
終了時に bridge、実席、room server、使い捨て project を harness の `finally` で撤去した。

## 正負の測定結果

1. 実席への DM `[t4-live-boot]` を bridge で届け、席が role・工程・room log を読み
   `[t4-live-boot-ok]` を返した — PASS。
2. 親の抑制的な発言後も、実席が `[t4-live-progress]` を全体へ報告し、親の配車なしに
   `[claim] t4-live-ready` を投稿した — PASS。`[配車]` は room に無かった。
3. 同名 member を残したまま `launch-seat.sh` を直接呼ぶ負例は
   `SEAT_ROOM_MEMBER_CONFLICT` で停止 — PASS。安全な launch 境界を確認した。
4. 完全一致の `[effort変更依頼]` DM を置かず、親が確定した自然文相当の理由と
   `--effort max` だけで `change-seat.sh` を実行。旧席撤去後に target metadata を読み返し、
   新席が再着任した — PASS。
5. 席変更後に `[t4-live-rejoin]` を送り、実席が role・工程・room log を読み直して
   `[t4-live-rejoin-ok]` を返した — PASS。
6. `member-autonomy-role-repro.mjs`、`parent-role-repro.mjs`、
   `codex-parent-delivery-repro.mjs`、`done-receipt-gate-repro.mjs` を同じ実行へ束縛した。
   ready / 文脈近接監査 / audit-before-done、Codex 親の配送境界、green item の親無講評、
   intentional defect の差し戻し順序を fixture で確認 — 全て PASS。

## focused test

- `node experiments/seat-change-repro.mjs` — `28/28 green`（履歴読返し不一致負例を含む）
- `node experiments/effort-change-repro.mjs` — `5/5 green`
- `node experiments/member-autonomy-role-repro.mjs` — green
- `node experiments/parent-role-repro.mjs` — green
- `node experiments/codex-parent-delivery-repro.mjs` — green
- `node experiments/done-receipt-gate-repro.mjs` — `29/29 green`
- `node --check experiments/autonomy-lifecycle-real-repro.mjs` — green
- `git diff --check` — green

最終 live report は `passed: true`、live checks 5/5。bridge末尾は room catch-up と
`t4-live-worker` への起床だけで、`DESCRIPTOR_MISSING` / `WAKEUP_BRIDGE_DELIVERY_FAILURE` は無かった。

## 判定の境界

実席の active / rejoin / seat change は実 Codex で測り、ready が無い席の監査順序・Codex 親の
工程管理境界・green/defect の親通知分岐は既存の focused fixture で測った。fixture の成功を
実席の成功へ置き換えてはいない。

## 状態

`58d648c` のr1 peer auditは履歴読返し欠落のDEFECTだった。`526ccc3` と `eea8323` のr1限定再監査を
先に完了し、DEFECT-FREEを確認してから、Reiへ `99f847a` と今回のt4証跡・実席live reportを
read-only peer audit依頼してt4をdoneにする。
