# t5 最終試験結果 — 正典同期・回帰・配布面

## 実施

- t4 の実席証跡を根拠に、`docs/plan.md` へ決定83を追加した。
  自然文の model / effort 依頼、同一vendor内の同一session変更、再起動時の再着任、
  作業者の自己試験・自己監査、監査担当による提出結果だけの判断とcloseを正典化した。
- 決定60・67と決定68の監査席に関する旧記述を、決定80・83で置換済みと明記した。
  決定68の「readyが無い席は仕事を発明しない」は維持した。
- 要求intakeを完了済みの履歴として更新し、README（日英）をCodex設定入口と現在の実席ライフサイクルへ同期した。

## 検証結果

- `node experiments/seat-change-repro.mjs` — 28/28 green。
- `node experiments/parent-role-repro.mjs` — green。
- `PEERTABLE_URL= node room/client.mjs diagnostics` — ready（room reachability は未設定のため not_applicable）。
- `npm pack --dry-run` — `peertable@0.3.9`、41 files。README・room・skill が梱包対象であることを確認。
- `lattice todo verify --plan peertable-autonomy-runtime-20260811 --json` — reconciled、`snapshot_stale=false`。
- 対象差分の `git diff --check` — green。

## SKILL受入の確認

- `skill/SKILL.md` 6.7 は、本人の自然文DM、親によるtarget確定、完全一致の再送・機械検査をしないこと、
  同一vendor内のmodel / effort変更が同一session・会話contextを保つことを明記している。
- 同ファイルの「親の operating notes」は、親が作業者・監査担当を代行しないこと、作業者が自己試験・
  自己監査を完了して最終結果だけを渡すこと、監査担当が試験を再実行せず妥当性を判断してcloseすることを
  明記している。
- よって、t5受入が指定するexact effort protocolと旧親 operating notesのsupersedeは、追加修正なしで
  現行SKILLに成立している。上記の非live focused検証は、この記載が案内する席設定変更・親roleの実装面を
  確認済みである。

## スコープ

- `autonomy-lifecycle-real-repro.mjs` は、t4で実席成立後に残った誤assertを測る使い捨てliveハーネスであり、t5の回帰対象外である。今回の再実行と一時的な変更は停止・撤回した。
- publish、version bump、本番deployは実施していない。
