# e1 明示宛先プロトコル

## 実施

- room server は宛先省略、`to: "all"`、配列内の `all` を HTTP 400 の typed error
  `peertable.error.v1 / EXPLICIT_RECIPIENT_REQUIRED` で拒否する。error にメンバー一覧は含めない。
- 複数人宛は `to_names` のみを保存し、新規 `to: "all"` 行を作らない。既存ログの
  `to: "all"` 行は書き換えず `read_log` 互換を維持する。
- MCP client、起床ブリッジ、run bridge、親着卓、teardown、SKILL と role templates を
  明示宛先だけの契約へ揃えた。broadcast の代替は作らず、状況把握は room log の pull とした。
- 新規memberの system 発言は本人宛、run bridge の宛先不明は
  `RUN_BRIDGE_RECIPIENT_UNKNOWN` としてlocal logへ残す。

## focused test

- `node experiments/explicit-recipients-repro.mjs`: green。broadcast 4形のtyped拒否、errorの
  member一覧非包含、単独/複数宛先、既存broadcast行の無変更読出し、新規broadcastゼロ、
  全bridge/template面を確認。
- `node experiments/bridge-catchup-repro.mjs 18901`: green。明示宛先だけでSSE catch-up、
  2件回収、配達判断まで実行。
- 一時room serverに対する
  `node experiments/cursor-repro.mjs room/client.mjs http://127.0.0.1:18902`: green。
  明示DMの未読をpost後にも取りこぼさない。
- 変更した8本の`.mjs`へ`node --check`、2本のshellへ`bash -n`、`git diff --check`:
  すべてgreen。
- `PEERTABLE_URL= node room/client.mjs diagnostics --json`:
  `overall: ready`（version/bin/runtime/skill bundleすべてpass、room到達性はnot_applicable）。

## 独立監査補正

Kanade監査で、配布templateは更新済みでも`docs/plan.md`の現行規律・通信契約・operating手順が
旧broadcastを案内している欠落を検出し、全面scanでREADMEの同型も見つけた。歴史実測は残し、現行節を明示宛先＋room log pullへ
改訂し、§16が決定62の`all`受理・`to: "all"`正規化・混在展開をsupersedeすると明記した。
既存のrun-bridge e2e 2本は廃止済みの配車CLIを測る歴史ハーネスであり、現行bridgeへは適用しない。
代わりに`run-bridge-explicit-recipients-repro.mjs`を追加し、既知介入は本人だけへDM、宛先不明は
typed local error、run進行はlocal logだけ、roomへbroadcast代替ゼロを現行CLIで固定した。
旧2本も一度実行したが、どちらも廃止済みspool/seat引数を現行bridgeが
`RUN_BRIDGE_ARGS_INVALID`で拒否してredになったため、今回のgreen根拠には数えていない。旧ファイル自体は変更しない。

Kanadeの独立再監査（room [1813]）は残存findingなしでgreen。旧server `2e5904d`へ現reproを当てた
負側ではbroadcast拒否・保存・system宛先がred、現headでは明示宛先repro、run-bridge専用repro、
catch-up、cursor実server、syntax、diagnostics、diff-checkがgreen。監査用port/temp/processは終了時ゼロ。

## 境界

親watchdogのlive commandはrepo所有processではないため、witnessではexternal process unknownを残す。
正本のfilter例は親本人への明示DMだけを通す形へ更新した。本番processの再接続は後続e2で実測する。
