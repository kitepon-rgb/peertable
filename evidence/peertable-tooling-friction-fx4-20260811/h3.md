# h3 Codex席の必須room MCP分離

## 実装

- 実装 commit: `1e37a7f6b64a331f80aab94d89ba93c2786c1250`
- h3対象: `skill/scripts/launch-seat.sh`、`experiments/mcp-room-isolation-repro.mjs`
- Codexヘッダや無関係MCP warningだけでは成功にせず、room member登録を観測した後だけ`room ready`へ進む。
- 起動前に同名memberを読み、残存時は`SEAT_ROOM_MEMBER_CONFLICT`、一覧読取不能時は`SEAT_ROOM_MEMBER_STATE_UNREADABLE`でtmux停止前にtyped failureとする。旧席を別席のまま自動削除しない。
- room登録未成立は`SEAT_ROOM_MCP_NOT_READY`として既存rollbackへ渡し、tmux・member・seat identity・credentialの残存を確認する。

## 独立監査

Gaussのnative read-only refuterがr1で、同名stale memberを名前一致だけでready扱いする欠陥を差し戻した。修正後のr3は、stale判定をtmux停止前へ移動し、indexへstale conflict検証を含むfixtureを反映した現物を確認してPASSとした。foreignなkanade/k1 Stop-hook差分はh3判定から分離した。

## 実測

- `node experiments/mcp-room-isolation-repro.mjs`: `7/7 green`
  - 無関係MCP warning併存＋room登録成功
  - 実`room/client.mjs`のroom read/post
  - stale同名memberの着席前conflict
  - member未登録時のtyped failure・rollback
  - Codex room MCP command/args/env、credential実値非露出
- `node experiments/launch-brief-repro.mjs`: `10/10 green`
- `bash -n skill/scripts/launch-seat.sh`: green
- `node --check experiments/mcp-room-isolation-repro.mjs`: green
- cached/worktree `git diff --check`: green
- Lattice h3 note sequence 10へ差戻し修正・再監査結果を記録済み。

## commit境界の注記

対象外pathはcommitへ入っていないが、`git commit -- <paths>`が同一`launch-seat.sh`のunstaged内容も取り込んだため、commitにはkanade/k1所有のStop-hook foreign hunkも共存する。foreign差分は戻さず、履歴改変も行わない。h3の受入判定・focused test・Lattice証跡は上記2 hunkとfixtureへ限定する。

publish・deploy・pushは実施していない。共有branchの他工程WIPとorigin divergenceを保ったまま、公開着地は保留する。
