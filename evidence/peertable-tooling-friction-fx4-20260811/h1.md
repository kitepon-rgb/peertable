# h1 teardownのroom URL表示をbyte完全にする

## 実装

- 実装 commit: `8025274cd28493fbf483e6c3e9701a61b0bb3f31`
- 対象: `skill/scripts/teardown.sh`、`experiments/teardown-room-url-repro.mjs`
- teardown報告のURLを`（${url}/${room}）`としてshell変数境界を閉じた。member解除、room原本保持、archiveの意味は変更していない。
- 後続h5でfixtureのmutation終了を`process.exitCode`へ変え、負例でも`finally`の一時dir撤去を通す現行形になっている。

## focused実測

- `bash -n skill/scripts/teardown.sh`: green。
- `node --check experiments/teardown-room-url-repro.mjs`: green。
- `node experiments/teardown-room-url-repro.mjs`: green。ASCII `ascii-room` と日本語 `卓-あ` の双方で表示URLが期待bytesと一致した。
- 各roomでmember DELETE 1回、room DELETE 0回、解散POST 1回、archive控え1件を確認した。
- `--without-boundary`: rc 1。旧`$url/$room）`変異をbyte完全assertionが捕捉した。
- `--without-unicode-path`: rc 1。後続h5のUnicode API path変異も捕捉し、h1現行fixtureへの退行がない。
- 正例・両負例のfixture/mutant一時dir残骸は0。対象2 pathはclean、`git diff --check`もgreen。

## 独立監査

別席のread-only監査はcommit実物と現行clean pathsを再読し、focused正例・両負例を独立再実行してdefect-freeと判定した。`8025274`単体時点の負例cleanup不足は後続h5差分で既に解消され、現行実測でも残骸なしを確認した。

pushは共有mainが`origin/main`に対して5 behind / 103 aheadで他工程の未受入commitを含むため、所有外成果を巻き込まないよう統合席のremote同期まで保留する。
