# t1: socket 解決と観測記述子の集約

## 実装

- `defaultTmuxSocket` と、明示設定・既定・検証済み発見を順に評価する `resolveTmuxSocket` を追加した。
- member の `observe` 記述子を優先し、旧 `peer-<name>` を後方互換として返す
  `resolveSeatObservation` を追加した。
- bash から同じ判定を使う薄い入口 `tmux-socket.mjs` と、socket 発見・記述子・既存 socket
  回帰の再現ハーネスを追加した。

## 確認

- 変更前に、`TMPDIR` 未設定で `resolveTmuxSocket({})` が
  `undefinedclaude-tmux-sockets/claude.sock` を返す欠陥を再現した。
- `node experiments/seat-socket-discovery-repro.mjs` — 7/7 green。
- `node experiments/seat-observe-descriptor-repro.mjs` — 4/4 green。
- `node experiments/seat-status-socket-repro.mjs` — 4/4 green。
- `git diff --check` — 問題なし。
