# t3: bridge supervisor

- `ensure-bridge.sh` を追加し、bridge を専用 tmux セッションで起動して既存の記録ファイルにある `ready_at` まで待機する形にした。
- `setup.sh` の `nohup` 起動をこの入口へ置換した。失敗は標準エラーに出し、scaffold 自体は完走する。
- `bash -n skill/scripts/ensure-bridge.sh skill/scripts/setup.sh` で構文を確認した。

残る実tmuxハーネスは t2 の `ready_at` 実装と結合して確認する。
