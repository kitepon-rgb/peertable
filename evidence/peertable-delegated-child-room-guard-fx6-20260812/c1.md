# c1 メンバー起点の子をPeertable席へ登録させない

## 実装

- `skill/scripts/launch-seat.sh` の実行境界は Kanade の `4f13019b9aa101a63450fd59edb16d2e379e4667` に含まれる。非空の `PEERTABLE_MEMBER` を `SEAT_LAUNCH_DELEGATED_CHILD_FORBIDDEN` で、credential、tmux、identity、room、brief の副作用より前に拒否する。
- c1固有の文書・fixtureは `ad4a6b0734ecd7b497441351cee2c22ae847a384` に、`skill/SKILL.md`、`skill/templates/member.md`、`experiments/seat-delegation-policy-repro.mjs` の3 pathだけを記録した。
- 親の正式増員は既存preflightへ到達し、メンバー自身のnative sub-agent、Aiterm外部agent、相談agentの利用は維持した。複雑な席階層・昇格制度は追加していない。
- `launch-seat.sh` に残る別作業a3の未コミットWIPと、Kanadeのcredential handoff変更は変更・revertしていない。

## focused実測

- `node experiments/seat-delegation-policy-repro.mjs`: rc=0。委譲子はtyped reject、preflight marker空、project副作用なし。親は既存preflightへ到達。
- `bash -n skill/scripts/launch-seat.sh`: rc=0。
- `git diff --check`（対象変更）および `git --no-pager diff ad4a6b0^ ad4a6b0 --check`: rc=0。
- `node experiments/seat-delegation-policy-repro.mjs --without-policy`: 旧ポリシー変異を検出し、期待どおり `negative-control-exit=1`。

## 文脈近接の別席監査

Akari が room [601] で read-only peer audit を実施した。`ad4a6b0` の3 path、`4f13019` のguard位置、child/parentの実起動、memberの委譲手段、path scopeを確認し、defect-free・受入をblockする欠陥なしと判定した。変更・commit・Lattice書込は行っていない。

## Lattice

- c1は actor 未解決の既知j1配線欠陥を修理せず、actor 3点を明示して `todo start` 済み。
- `lattice todo structure realize --planned` 成功。planned digestは `7471839d73af4351623254273f4c220435257c224d61d058ea42653244830d6c`、realization digestは `10225c2206744323eac8bed4a164d6202e5ce186b7aa469e6295e35e2794b18e`。

publish・本番deployはこのc1では実施していない。
